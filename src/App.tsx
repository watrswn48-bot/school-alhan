import React, { useEffect, useState } from 'react';
import { Users, Church, BookOpen, BarChart3, Sliders, UserCheck, GraduationCap, LockKeyhole, BellRing, UserPlus } from 'lucide-react';
import { UserSession, Student } from './types';
import { initStorage, getStudents, getSchoolLogo, getServants, getLectures, saveStudent } from './services/storage';
import { normalizeRole, normalizeServantPermissions, sessionHasPermission } from './services/permissions';
import { lecturesNeedingEvaluation } from './services/schoolSystem';
import { normalizeSchoolClass } from './services/schoolClassUtils';
import { runAutomaticRegularSchoolPromotion } from './services/academicYearService';
import { Navbar } from './components/Navbar';
import { LoginModule } from './components/LoginModule';
import { ClassesAndStudentsModule } from './components/ClassesAndStudentsModule';
import { AddStudentModule } from './components/AddStudentModule';
import { FridayLiturgyModule } from './components/FridayLiturgyModule';
import { LectureSystemModule } from './components/LectureSystemModule';
import { AnalyticsModule } from './components/AnalyticsModule';
import { AdminPanelModule } from './components/AdminPanelModule';
import { CurriculaModule } from './components/CurriculaModule';
import { ResultsEntryModule } from './components/ResultsEntryModule';
import { CumulativeProfileModal } from './components/CumulativeProfileModal';
import { SmartIDCardModal } from './components/SmartIDCardModal';
import { SiteFooter } from './components/SiteFooter';

const SchoolBackgroundWatermark: React.FC<{ logo: string }> = ({ logo }) => <div className="fixed inset-0 pointer-events-none z-0 overflow-hidden flex items-center justify-center select-none"><div className="w-[500px] h-[500px] sm:w-[700px] sm:h-[700px] lg:w-[850px] lg:h-[850px] max-w-[90vw] max-h-[90vh] rounded-full overflow-hidden opacity-[0.16] border-4 border-amber-400/20 shadow-2xl transition-all duration-700"><img src={logo} alt="" className="w-full h-full object-cover" referrerPolicy="no-referrer" /></div><div className="absolute inset-0 bg-[radial-gradient(circle_at_center,rgba(245,158,11,0.06)_0%,rgba(15,23,42,0)_70%)]" /></div>;
type NavTab = 'class1'|'class2'|'class3'|'class4'|'class5'|'class6'|'class7'|'class8'|'admin';

export default function App() {
  const [dataVersion,setDataVersion]=useState(0);
  const [session,setSession]=useState<UserSession>(()=>{const saved=localStorage.getItem('deacon_system_session_v1');if(saved){try{return JSON.parse(saved);}catch{}}return{isLoggedIn:false,mode:'servant'};});
  const [schoolLogo,setSchoolLogo]=useState(()=>getSchoolLogo());
  const [activeNavTab,setActiveNavTab]=useState<NavTab>('class1');
  const [studentPortalTab,setStudentPortalTab]=useState<'profile'|'curricula'>('profile');
  const [selectedStudentForProfile,setSelectedStudentForProfile]=useState<Student|null>(null);
  const [selectedStudentForIDCard,setSelectedStudentForIDCard]=useState<Student|null>(null);
  const [newStudentIDCard,setNewStudentIDCard]=useState<Student|null>(null);

  useEffect(()=>{const f=()=>setSchoolLogo(getSchoolLogo());window.addEventListener('school_logo_updated',f);return()=>window.removeEventListener('school_logo_updated',f);},[]);
  useEffect(()=>{initStorage(()=>setDataVersion(v=>v+1)).then(async()=>{try{const legacy=getStudents(true).filter(s=>s.schoolClass==='إعدادي'||s.schoolClass==='ثانوي');legacy.forEach(s=>saveStudent({...s,schoolClass:normalizeSchoolClass(s.schoolClass)}));if(legacy.length)setDataVersion(v=>v+1);const r=runAutomaticRegularSchoolPromotion();if(r.processed)setDataVersion(v=>v+1);}catch(e){console.warn('Startup migration/academic transition:',e);}});},[]);
  useEffect(()=>{localStorage.setItem('deacon_system_session_v1',JSON.stringify(session));},[session]);
  useEffect(()=>{const hide=()=>{document.querySelectorAll('button').forEach(b=>{if(b.textContent?.trim()==='العودة للرئيسية')b.remove();});};hide();const obs=new MutationObserver(hide);obs.observe(document.body,{childList:true,subtree:true});return()=>obs.disconnect();},[]);

  const normalizeSession=(incoming:UserSession):UserSession=>{if(incoming.mode!=='servant'||!incoming.userId)return incoming;const srv=getServants().find(s=>s.id===incoming.userId);const role=normalizeRole(srv?.role||incoming.role||(incoming.userId==='srv-admin-01'?'admin':'junior_servant'));return{...incoming,role,fullName:srv?.fullName||incoming.fullName,permissions:normalizeServantPermissions(role,srv?.permissions||incoming.permissions)};};
  const handleLoginSuccess=(s:UserSession)=>{const n=normalizeSession(s);setSession(n);if(n.mode==='student'){const st=getStudents().find(x=>x.id===n.userId||x.studentCode===n.studentCode);if(st)setSelectedStudentForProfile(st);}};
  const handleLogout=()=>{setSession({isLoggedIn:false,mode:'servant'});localStorage.removeItem('deacon_system_session_v1');setSelectedStudentForProfile(null);setSelectedStudentForIDCard(null);setNewStudentIDCard(null);setActiveNavTab('class1');};
  if(!session.isLoggedIn)return <LoginModule onLoginSuccess={handleLoginSuccess}/>;
  if(session.mode==='student'){
    const student=selectedStudentForProfile||getStudents().find(s=>s.id===session.userId||s.studentCode===session.studentCode);
    return <div className="min-h-screen bg-slate-900 text-slate-100 font-['Tajawal'] pb-12 relative overflow-x-hidden"><SchoolBackgroundWatermark logo={schoolLogo}/><div className="relative z-10"><Navbar session={session} onLogout={handleLogout}/><main className="max-w-7xl mx-auto px-3 sm:px-6 lg:px-8 py-5 sm:py-8 space-y-6"><div className="bg-slate-950 border border-slate-800 rounded-3xl p-4 sm:p-6 shadow-xl flex flex-col sm:flex-row sm:items-center justify-between gap-3"><div className="flex items-center gap-3"><div className="p-3 bg-amber-500/20 text-amber-400 rounded-2xl border border-amber-500/30"><UserCheck className="w-6 h-6"/></div><div><h2 className="text-base sm:text-xl font-black text-slate-100">بوابة متابعة الطالب وولي الأمر</h2><p className="text-xs text-slate-400">قراءة سجلات الحضور والمحاضرات والنتائج</p></div></div><div className="flex gap-2 flex-wrap"><button onClick={handleLogout} className="px-4 py-2 bg-rose-500/20 text-rose-300 rounded-xl text-xs font-bold">تسجيل الخروج</button>{student&&<button onClick={()=>setSelectedStudentForIDCard(student)} className="px-4 py-2 bg-slate-800 text-sky-400 rounded-xl text-xs font-bold">كارنيه الطالب</button>}</div></div><div className="bg-slate-950 p-1.5 rounded-2xl border border-slate-800 flex items-center gap-2 overflow-x-auto"><button onClick={()=>setStudentPortalTab('profile')} className={`px-5 py-2.5 rounded-xl text-xs font-bold shrink-0 ${studentPortalTab==='profile'?'bg-amber-500 text-slate-950':'text-slate-400'}`}><UserCheck className="w-4 h-4 inline ml-2"/>سجلي التراكمي</button><button onClick={()=>setStudentPortalTab('curricula')} className={`px-5 py-2.5 rounded-xl text-xs font-bold shrink-0 ${studentPortalTab==='curricula'?'bg-amber-500 text-slate-950':'text-slate-400'}`}><BookOpen className="w-4 h-4 inline ml-2"/>المناهج والألحان</button></div>{student?(studentPortalTab==='profile'?<div className="bg-slate-900 border border-slate-800 rounded-3xl p-2 sm:p-6 shadow-xl"><CumulativeProfileModal student={student} isOpen={true} onClose={handleLogout} onLogout={handleLogout} session={session} onGenerateIDCard={s=>setSelectedStudentForIDCard(s)}/></div>:<CurriculaModule session={session} studentLevel={student.level} studentYear={student.year} isStudentPortal={true}/>):<div className="p-12 text-center text-slate-400">لم يتم العثور على سجلات هذا الطالب.</div>}</main><SiteFooter /></div><SmartIDCardModal student={selectedStudentForIDCard} isOpen={!!selectedStudentForIDCard} onClose={()=>setSelectedStudentForIDCard(null)}/></div>;
  }
  const isAdministrator=session.role==='admin'||session.userId==='srv-admin-01';
  const canOpenAdmin=isAdministrator||sessionHasPermission(session,'canViewAdminPanel');
  const canStudents=isAdministrator||sessionHasPermission(session,'canAddEditStudents');
  const canAnalytics=isAdministrator||sessionHasPermission(session,'canViewAnalytics');
  const canCurricula=isAdministrator||sessionHasPermission(session,'canManageCurricula')||sessionHasPermission(session,'canUploadFiles');
  const canGrades=isAdministrator||sessionHasPermission(session,'canManageGrades');
  const canLectures=isAdministrator||sessionHasPermission(session,'canManageLectures')||sessionHasPermission(session,'canEvaluateLectures');
  const lectureNotifications=canLectures?lecturesNeedingEvaluation(getLectures(),session.userId,isAdministrator):[];
  const Guard:React.FC<{allowed:boolean;children:React.ReactNode}>=({allowed,children})=>allowed?<>{children}</>:<div className="bg-slate-900 border border-slate-800 rounded-3xl p-10 text-center space-y-3"><LockKeyhole className="w-12 h-12 mx-auto text-amber-500"/><h3 className="text-lg font-bold">هذه الوظيفة غير متاحة لحسابك</h3><p className="text-xs text-slate-400">يمكن لأبونا فتح هذه الصلاحية من إدارة الخدام.</p></div>;
  const nav=(tab:NavTab,icon:React.ReactNode,label:string,allowed=true)=><button onClick={()=>allowed&&setActiveNavTab(tab)} disabled={!allowed} className={`px-4 py-3 rounded-2xl text-xs font-bold transition-all flex items-center gap-2.5 shrink-0 ${activeNavTab===tab?'bg-amber-500 text-slate-950 shadow-lg':'text-slate-400 hover:text-slate-100 hover:bg-slate-900'} ${!allowed?'opacity-40 cursor-not-allowed':''}`}>{icon}<span>{label}</span></button>;
  return <div className="min-h-screen bg-slate-900 text-slate-100 font-['Tajawal'] pb-16 selection:bg-amber-500 selection:text-slate-950 relative overflow-x-hidden"><SchoolBackgroundWatermark logo={schoolLogo}/><div className="relative z-10"><Navbar session={session} onLogout={handleLogout} onRefreshData={()=>setDataVersion(v=>v+1)}/><main className="max-w-7xl mx-auto px-3 sm:px-6 lg:px-8 py-4 sm:py-6 space-y-5">{lectureNotifications.length>0&&<button onClick={()=>setActiveNavTab('class3')} className="w-full text-right bg-amber-500/10 hover:bg-amber-500/15 border border-amber-500/40 rounded-2xl p-4 flex items-start gap-3"><BellRing className="w-5 h-5 text-amber-400 shrink-0"/><div><div className="font-black text-amber-300">لديك {lectureNotifications.length} محاضرة تحتاج تقييم الطلاب</div><div className="text-xs text-slate-300 mt-1">اضغط لفتح المحاضرات وتقييم الطلاب الذين حضروا محاضرتك.</div></div></button>}<div className="bg-slate-950/90 border border-slate-800 p-2 rounded-3xl shadow-xl overflow-x-auto"><div className="flex items-center gap-1.5 min-w-max">{nav('class1',<Users className="w-4 h-4"/>,'الفصول والطلاب',canStudents)}{nav('class8',<UserPlus className="w-4 h-4"/>,'إضافة الطلاب',canStudents)}{nav('class2',<Church className="w-4 h-4"/>,'حضور القداس',true)}{nav('class3',<BookOpen className="w-4 h-4"/>,'المحاضرات',canLectures)}{nav('class4',<BarChart3 className="w-4 h-4"/>,'الإحصائيات',canAnalytics)}{nav('class5',<BookOpen className="w-4 h-4"/>,'المناهج',canCurricula)}{nav('class6',<GraduationCap className="w-4 h-4"/>,'النتائج',canGrades)}{canOpenAdmin&&nav('admin',<Sliders className="w-4 h-4"/>,'لوحة الإدارة',true)}</div></div><div className="min-h-[70vh]">{activeNavTab==='class1'&&<Guard allowed={canStudents}><ClassesAndStudentsModule session={session} onSelectStudentProfile={s=>setSelectedStudentForProfile(s)} onGenerateIDCard={s=>setSelectedStudentForIDCard(s)}/></Guard>}{activeNavTab==='class8'&&<Guard allowed={canStudents}><AddStudentModule session={session} onStudentSaved={setNewStudentIDCard}/></Guard>}{activeNavTab==='class2'&&<FridayLiturgyModule session={session}/>} {activeNavTab==='class3'&&<LectureSystemModule session={session}/>} {activeNavTab==='class4'&&<Guard allowed={canAnalytics}><AnalyticsModule session={session} onSelectStudentProfile={s=>setSelectedStudentForProfile(s)}/></Guard>} {activeNavTab==='class5'&&<Guard allowed={canCurricula}><CurriculaModule session={session}/></Guard>} {activeNavTab==='class6'&&<Guard allowed={canGrades}><ResultsEntryModule session={session}/></Guard>} {activeNavTab==='admin'&&<Guard allowed={canOpenAdmin}><AdminPanelModule session={session}/></Guard>}</div>{selectedStudentForProfile&&<CumulativeProfileModal student={selectedStudentForProfile} isOpen={!!selectedStudentForProfile} onClose={()=>setSelectedStudentForProfile(null)} session={session} onGenerateIDCard={s=>setSelectedStudentForIDCard(s)}/>}<SmartIDCardModal student={selectedStudentForIDCard} isOpen={!!selectedStudentForIDCard} onClose={()=>setSelectedStudentForIDCard(null)}/><SmartIDCardModal student={newStudentIDCard} isOpen={!!newStudentIDCard} onClose={()=>setNewStudentIDCard(null)}/></main><SiteFooter /></div></div>;
}
